import {styled} from "@tamagui/core";
import {H2} from "tamagui";

const AvailableGamesTitle = styled(H2, {
  name: "AvailableGamesTitle",
  color: "#ffffff",
  fontSize: 32,
  fontWeight: "bold",
  marginTop: 25,
});

export default AvailableGamesTitle;
