{
  "name": "@game/colyseus-schema",
  "version": "0.0.0",
  "private": true,
  "description": "Package with Colyseus classes and types",
  "main": "index.ts",
  "author": "Aaron van den Berg",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "typescript": "^4.9.5",
    "@colyseus/schema": "^1.0.45"
  }
}
